fatload mmc 0:$mmcloadpart $fpgadata soc_system.rbf;
fpga load 0 $fpgadata $filesize;
run bridge_enable_handoff;
run mmcload;
echo "+-+-+-+-busyboxfs+-+-+-"
setenv busyboxroot /dev/mmcblk0p5
setenv bootargs console=ttyS0,115200 root=${busyboxroot} ro rootwait dofsck init=/init
bootz ${loadaddr} - ${fdtaddr}
